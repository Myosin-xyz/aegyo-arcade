# Release checkpoint — September 15, 2026

This checkpoint supersedes the September 12 dependency status. It records bounded observations, not continuous monitoring or production activation.

## Email delivery is working

Resend reports `aegyoarena.com` **Verified**. The observed authoritative DNS contains its DKIM TXT and the `send` subdomain MX/SPF records. The existing sending-only staging key is now restricted to this domain. Accounts staging uses `accounts@aegyoarena.com`, Resend mode, and signup disabled; configuration-only deployment `7bb806a0-7975-46b4-95bb-a8382689062f` succeeded with runtime source `3841cf5`.

A dedicated dated alias in Mateo's Myosin mailbox received both verification and recovery messages in Gmail **Inbox**. Gmail displayed `send.aegyoarena.com` as mailed-by, `aegyoarena.com` as signed-by, and TLS. Both actual mailbox links were exercised:

- Verification marked the exact staging fixture verified; both retained provider sessions reflected it.
- Recovery invalidated both retained provider sessions; the old password returned HTTP 401.
- Reopening the used recovery link returned `INVALID_TOKEN` and the invalid/already-used page.
- The new password reached the signed-in page with “Email verified.” Signout completed.
- A final exact-fixture database read confirmed verified email, a persisted password-change timestamp, and zero sessions.

Temporary remote proof files and local retained cookies/old password were removed. The staging identity remains available for future recovery-based tests. No ordinary user's password was changed. This run did not repeat product-local reset tests or prove production email configuration, future inbox placement, or campaign sending capacity.

## Access and preservation baseline

Mateo accepted the pending repository invitation. GitHub now reports push access to `Francisgood/kpop-lyrics`. The initial adapter PR #12 was subsequently merged and deployed with shared authentication disabled, as recorded below.

A dedicated nonprivileged legacy canary was created through Aegyo's actual production signup route with newsletter subscription explicitly false. Its old password was accepted by the actual login route for the exact same source ID, and both resulting sessions were signed out. Its unverified email status must remain unchanged during migration. Its password and source ID are kept only in the ignored private handoff file.

The subsequent read-only inventory found **54 users, 26 sessions and 48 public tables**. These counts are a checkpoint, not a substitute for complete row reconciliation or a frozen production cutover snapshot. The naturally occurring population can change before cutover.

Railway's normal backup listing contains backup `eeac675a-427d-4a5c-b85b-304f6753808b`, named `pre-shared-auth-rehearsal-20260915`, created `2026-09-15T14:20:15.005Z` (261 MB referenced). The workflow-status endpoint refused authorization; the recorded evidence is the authorized backup-list result. Restore usability must be proved separately.

## Remaining release gates

1. **Accounts hostname:** authoritative DNS still lacks `account.aegyoarena.com`. Add the accepted DNS-only CNAME `account` → `d5d1smmz.up.railway.app`; `2tmkqmk3.up.railway.app` belonged to the old empty placement and is superseded. Resend DNS is a different set of records and is complete.
2. **Final production migration:** the restored-data rehearsal verified 54 imported users, 26 retained legacy sessions and the original 48-table restore. A fresh corrected ownership rehearsal has now passed, as recorded below. Production still requires a fresh coordinated credential-writer freeze, snapshot, import, mapping reconciliation and activation; a successful clone does not migrate the live products.
3. **Aegyo adapter release:** the additive production schema is installed and verified; mappings/latch remain empty. PR #12 was merged as `042fc17ae2687a8e4c504f94156e0261844af8b0`, with the adapter still disabled. Railway deployment and the existing-password canary passed; the final shared-auth cutover remains separate. Do not run the incompatible historical Prisma migration chain against production.
4. **Daebak ownership:** exercise the dual-session linking journey with a human-controlled existing Privy identity and its corresponding local row. Never link a real wallet identity to the shared synthetic test account. This fallback preserves Privy authorization but does not make Privy login automatic.
5. **Production cutover:** prepare production email, finish reconciliation, activate the same production issuer across the products, and run preservation and session smoke tests before opening the contest.
6. **Contest business decisions:** the user requested proposed defaults because rules and prizes are not finalized. The draft in [the team decision note](COMPETITION_TEAM_DECISION_NOTE.md) remains unapproved; material-prize competition stays disabled.

## Further work completed at this checkpoint

- PR #12 was updated to `4b0ef938837595f799d194e89b0610107c5a2ecb`. Its guarded additive installer was independently rerun against the saved production schema in isolated PostgreSQL 18: full catalog preservation, an unchanged Prisma journal, empty mappings/latch, and rerun refusal passed. That was the initial preflight. The later corrected production installation and PR merge are recorded below.
- The [staging competition lifecycle](STAGING_COMPETITION_LIFECYCLE_PROOF.md) passed positive scoring, daily-best replacement, public standings, immutable close/finalize, authenticated claims, and exact-retry fulfillment. The no-value synthetic round reconciled to two ledger deltas totaling 200 points, one final result, and one fulfilled synthetic award. Material contest approval is separate.
- The existing Daebak email login selected by Mateo succeeded in production and staging. A scoped Privy lookup and production read-only query confirmed one local user, the same derived/stored smart wallet, two grant records, and no referral codes/attributions. The guarded initializer was corrected for the existing RPC binding and a temporary restricted operator role. One exact owned identity is now prepared in staging. An independent database read confirmed one user, zero bindings, and zero grant/referral/beta/notification rows. The browser reached “Sign in to Aegyo Accounts”; human Accounts sign-in and explicit linking remain pending. No financial transaction or grant was requested.
- Two new empty rehearsal databases were created on the existing private restored PostgreSQL service, preserving the September 12 clone. Their separate owner/reader/runtime roles are restricted; public database access is revoked. No additional Railway account, service or public proxy was created for them. Both the literal wrapper proof and remote execution passed; see the observed result below.

## Real restored-data import passed

The initial inspect restored all 48 tables and verified full original-row fingerprints. Its subsequent snapshot step refused a CA-versus-leaf fingerprint mismatch. The preserved clone remained intact. Correcting the independently observed leaf pin and running the read-only resume produced the approved 54-user snapshot without copying production a second time.

Clone-only apply deployment `be03f22f-0a3f-43e2-8242-bd69b9ab11ba` succeeded once. Independent reads found 54 Accounts users, 54 credential accounts, 54 import identities, one import batch, 54 Aegyo mappings, and one clone activation latch. The clone retained 54 users and 26 sessions. The canary kept the exact mapping, unverified email and ordinary user role; its existing password worked through the restricted Accounts runtime role. The full restore fingerprints, identity mapping and canary checks passed. A later review found the separate ownership collector silently skipped CamelCase tables, so the earlier ownership comparison does not establish linked-record coverage. Its schema-specific correction and fresh evidence are recorded below and in the operator record. Production was not connected during apply. Operator connection strings, passwords, pepper and canary secrets were cleared after acceptance. Full details are in [the real import operator record](REAL_AEGYO_IMPORT_OPERATOR.md).

## Production preparation and remaining human steps

The first additive production schema attempt rolled back completely when inconsistent heredoc escaping changed the separator in the preservation hashes. A separate read confirmed 54 users, 26 sessions and no partial auth tables. Commit `e8a89e7` uses `chr(10)` in both hashes, preserves sanitized failure metadata and adds a multirow Album regression. The parent independently reproduced the corrected PostgreSQL proof before a second attempt. That attempt succeeded with full original-row/column preservation. Independent production verification found 54 users, 26 sessions, zero mappings, zero latch rows, five nullable Session columns, two active latch-protection triggers, the expected operator checksum and four unchanged historical migration records.

After schema verification, the production Aegyo start override was changed from its non-idempotent seed command to `npm start`. The build override remains `npx prisma generate && next build`, with no pre-deploy command. The configuration change did not trigger a deployment. PR #12 was then merged at the reviewed head; Railway deployment `6a13e4b3-85bd-4ca9-bc4b-32f1ea2ab29b` succeeded with merge commit `042fc17ae2687a8e4c504f94156e0261844af8b0`. The public homepage and login page returned 200; the existing canary password signed in to the same local user ID and signed out. A separate post-deploy read confirmed 54 users, 26 sessions, no canary session, zero mappings and zero latch rows. Shared auth remains disabled.

Production Accounts now has the verified branded sender and selected Resend configuration saved without deployment. It reuses the existing domain-restricted sending-only key; production does not yet have an independent key. Traffic and signup remain false, and production delivery is still unproven. No paid plan was selected. Accounts DNS, the human-owned Daebak link, the final frozen production migration and approved contest rules remain the activation gates.

## Reviewable product branches

- [Arcade PR #2](https://github.com/Myosin-xyz/aegyo-arcade/pull/2) contains Accounts and the verified competition implementation. Its initial frozen-lockfile failure was corrected in `8048e84` without unrelated dependency upgrades. The Vercel preview and isolated Accounts Linux proof passed. Main CI formatting was corrected and typecheck, lint, format, database tests, production build and bundle budgets passed. The browser suite exposed an Aegyo Pop test that compared an animated translucent HUD; `b27640f` changes it to exact game-owned shot-count assertions while retaining real pointer input. Its targeted production mobile browser test and four module tests passed. The complete verify workflow `34991762240` then passed, and the Accounts Linux workflow `34991762335` passed after retrying a Docker Hub token-endpoint HTTP 500. Those results covered `4eb11b5`. The corrected ownership code at `56e6740` also passed full CI `34994817089`, isolated Accounts Linux proof `34994817092`, and Vercel preview. The PR remains draft.
- [Daebak PR #5](https://github.com/Myosin-xyz/daebak-markets/pull/5), head `8b226db5e61cd38054bdceba94613780ab69af51`, is draft with green Vercel preview and build/typecheck/lint/format/test/commitlint CI. Production remains unchanged. Its standalone binding-table SQL and production Drizzle journal review precede the final deploy. The existing Vercel migration guard skips non-production deployments.

## Production placement refinement

The completed clone import ran within one private Railway project. The original empty production Accounts service was in the separate Arcade project, which cannot privately connect to Aegyo's database. The existing importer requires both connections; a public PostgreSQL proxy is not the selected solution.

Production Accounts has been relocated into the existing Myosin Aegyo project/environment, with a separate dedicated PostgreSQL service and an independent deployment from the Arcade repository. This preserves the maintained import path without adding a custom encrypted-export protocol. Read-only preflight confirmed the old target has zero users/accounts/sessions/tokens and exactly three OAuth clients. Preserve those exact client IDs/secrets, provider secret and reader keys. Do not delete the old volume or transfer user data during provisioning. Keep both traffic and signup disabled until the final freeze/import/continuity gates pass. The accepted new service requires `account` → `d5d1smmz.up.railway.app`, DNS-only; Railway requests no additional TXT. The old empty Accounts application and its exclusive database deployments were stopped after rechecking their empty user state, while retaining services/configuration/volume for rollback. The new app deployed successfully; root independently verified `/healthz` 200 and intentional `/readyz` and `/sign-in` 503 responses with traffic/signup disabled. A separate root database read also confirmed zero users/accounts/sessions/tokens, three clients, schema version 1, guard revision 2 and both enabled triggers. The runtime role has no superuser/schema-create/protected-security-update/operator-revoke access. The provider secret, three client definitions, reader keys and domain-restricted Resend sender configuration exactly match the prior empty service. See [the relocation record](ACCOUNTS_PRODUCTION_RELOCATION_2026-09-15.md).

Daebak production's seven tracked migration hashes match its Drizzle journal exactly. The additive binding-table installation is now complete, as recorded below. Aggregate counts remain 15 users, 23 grants, four referral codes and three attributions. The current production runtime uses the existing broad database-owner role; its login integration is still disabled.

## Final cutover checks being tightened

- Aegyo PR #14 adds an explicit `AEGYO_AUTH_CUTOVER_FREEZE=true` override that remains closed even after the activation latch is inserted. It also makes legacy logout expire the browser cookie without deleting session rows while closed; normal logout behavior remains unchanged. Root independently reran the original 24 focused tests, both new poll-freeze tests, and typecheck successfully using the existing locked dependencies. PR #14 was merged as `46b595da26a84b0c594d7d78cf2f04af97243293`; Railway deployment `1caea3a2-0bb0-430c-a6b2-8e60333f9613` succeeded. Root exercised the existing-password canary on the actual site: homepage/login 200, exact local ID retained, session issued and signed out. An independent read confirmed 54 users, 26 sessions, zero canary sessions, zero mappings and zero latches. Both shared-auth and freeze flags remain false. The operator must also drain old requests and pause direct database writers before taking the final snapshot.
- The separate ownership collector queried unquoted CamelCase relation names, causing PostgreSQL to skip its linked tables. It also assumed column shapes that differ from production PollVote and Follow. This does not invalidate the full 48-table restore fingerprints, 54 exact account mappings, canary login or additive production schema preservation. It did invalidate the old collector’s linked-record claim. The corrected collector and fresh clone-only run below now provide nonempty coverage against the actual schema.

## Daebak production schema preparation

Railway’s normal backup listing records `afc85364-8a76-42d5-ab8e-83e9575a2d55`, `pre-shared-identity-binding-20260915`, created `2026-09-15T16:13:28.968Z` (2,198 MB referenced). The exact standalone SQL (`849406fd9ba9b71683f08bf7e4c9ebba524a447cff333f308e3445f1ebbf0b40`) was independently reviewed and installed once in a guarded transaction. Deterministic SHARE locks kept existing data stable while nine public/Drizzle tables were fingerprinted before and after. All original rows and the seven migration hashes were unchanged. A separate read verified zero bindings, the exact primary key, both uniqueness constraints and the user foreign key with NO ACTION behavior. Counts remained 15 users, 23 grants, four referral codes and three attributions; the public homepage returned 200. No product login or wallet behavior changed.

## Corrected ownership rehearsal accepted

Fresh inspect `678f2d11-c440-49ea-b80f-d0380a15ae1c` verified all 51 current source tables in a new private clone. Clone-only apply `21575df6-0520-4d5d-a102-b8c5d91131d2` passed the corrected normalized before/after ownership comparison, exact mapping activation and old-password canary. The real coverage includes 26 sessions, two favorites, 90 comments, 275 suggested edits, two follows and five anonymous poll votes; the other required ownership tables were explicitly checked and empty. Root independently verified 54 users/credentials/import identities, 54 local mappings, one import batch/latch, 26 legacy sessions, guard revision 2 and zero shared metadata on legacy sessions. The import snapshot digest is distinct from the ownership manifest digest; see [the corrected operator record](REAL_AEGYO_IMPORT_OPERATOR.md) for evidence boundaries and exact deployment references.

The one-shot operator is stopped and scrubbed. The fresh clone roles remain restricted and time-expiring for review. The separate temporary Daebak owned-identity operator role was revoked and dropped while human sign-in remains pending; its prepared staging identity is intact with one user, zero bindings and zero grants. Reprovision a bounded verification role when the user completes the link; do not repeat the staging identity initializer.

Production shared authentication and the real contest remain disabled. Remaining actions are the final Accounts CNAME/certificate, the human-owned Daebak linking journey, the coordinated live freeze/import/activation and post-cutover product checks, and team approval of contest rules/prizes.

## Human Daebak check: staging permission regression

The human-owned Accounts password reset and sign-in completed. The Daebak linking page then exposed a staging permission regression: its status query used `SELECT ... FOR SHARE` on `users`, while the restricted runtime deliberately has SELECT but no UPDATE/DELETE permission. Root reproduced SQLSTATE 42501 using the actual staging role, without changing rows. The owned identity and wallet commitment still match the recorded production identity; staging remains one user, zero bindings and zero financial/referral/notification rows. The corresponding Accounts email is verified.

Daebak commit `4c5a3da4137de533318dde5c54eca41bec106341` removes the unnecessary row lock and relies on the existing foreign key during binding insertion. Only that named FK's actual postgres-js `constraint_name` failure is classified as a concurrently missing user; unrelated failures propagate. The Luna worker ran 11 unit/real-PostgreSQL tests with no skips, including least-privilege status/link and a two-connection deletion race. Root reviewed the change and independently reran nine unit tests. Typecheck, lint, formatting, full GitHub CI `35002569495` and Vercel preview passed. No runtime user-write permission was granted.

The immutable tracked-file staging bundle references that exact commit. Railway deployment `13e5eb4d-7280-4054-a2a6-8e3c8b8d602e` succeeded. After reload, the same human browser journey now shows “Both accounts are verified” and the Link accounts action. A separate post-deploy read still confirms the exact identity/wallet, one user, zero bindings, zero financial rows and no runtime UPDATE/DELETE privilege on users. Explicit browser confirmation for creating the staging binding is pending; final linking and read-only post-link reconciliation have not yet run. Production remains unchanged.
